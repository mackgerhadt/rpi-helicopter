import RPi.GPIO as GPIO

version = "0.0.0 alpha"

print ("Welcome to Helicopter! Version " + version)

if (version == "0.0.0 alpha")
    print ("This is an early alpha. The helicopter may be unstable.")
